#include "main.h"

/**
  * print_alphabet - prints out all the alphabets in lower case
  * main - uses built in functions
  * Return: No return type
  */

void print_alphabet(void)
{
	char i = 'a';

	for (i = 'a'; i <= 'z'; i++)
	{
	_putchar(i);
	}
	_putchar('\n');
}
