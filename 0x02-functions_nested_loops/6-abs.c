#include "main.h"

/**
  * _abs - gets the absolute value of an input
  * @i: input whose absolute value is to be determined
  * Return: i or k
  */
int _abs(int i)
{
	const int j = 0;
	int k;

	if (i < 0)
	{
	k = j - i;
	return (k);
	}
	else
	{
	return (i);
	}
}
