#include <stdio.h>

/**
  * main - Entry point of the code
  * @argc: indicates the number of arguments in main
  * @argv: argument vector
  * Return: Always 0 (Success)
  */
int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", arg[0]);
	}
	return (0);
}
